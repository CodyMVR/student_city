{% extends 'base.html.twig' %}
{% block body %}
  <h1>Ajouter un lieu</h1>
  {{ form_start(form) }}
    {{ form_row(form.name) }}
    <button class="btn">Enregistrer</button>
  {{ form_end(form) }}
{% endblock %}
